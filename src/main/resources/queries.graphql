{
    order(id: 4370307900) {
        totalPrice
        orderItems {
            product {
                imageUrl
            }
            shipment {
                status
            }
        }
    }
}


mutation CreateOrder($order: OrderInput!) {
    createOrder(order: $order) {
        id
        totalPrice
    }
}